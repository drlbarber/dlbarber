
// Le site fonctionne en mode "Client-Only" pour garantir la stabilité visuelle.
// La connexion DB directe depuis le navigateur causait des crashs de dépendances.
// Nous utilisons le LocalStorage (sauvegarde locale) via BookingContext pour le moment.

export const sql = null;
